---
phase: 01-clean-platform-skeleton
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - fly.toml
autonomous: false
requirements: [PLAT-02, PLAT-04]
must_haves:
  truths:
    - "D-05: The herald-scraper-bot-test Fly app exists and is separate from prod herald-scraper-bot"
    - "D-06: A Fly (Managed) Postgres instance is attached and its DATABASE_URL is rewritten to postgresql+asyncpg for the app"
    - "D-08: A deploy succeeds and Alembic migrations run on release (deploy + boot + migrate)"
    - "D-07: The bot logs into the test Discord server and replies pong to an @mention (login + pong)"
    - "D-10: The new MA-beta Anthropic API key is set as a Fly secret (never committed) and MA config is wired/validated"
  artifacts:
    - path: "fly.toml"
      provides: "Final deploy config validated against a live deploy"
      contains: "herald-scraper-bot-test"
  key_links:
    - from: "Fly secret DAIMON_DATABASE__URL"
      to: "attached Fly Postgres"
      via: "postgres:// rewritten to postgresql+asyncpg://"
      pattern: "postgresql\\+asyncpg"
    - from: "Fly release_command"
      to: "packages/core/alembic"
      via: "alembic upgrade head on release"
      pattern: "alembic upgrade head"
---

<decisions_covered>
D-05: deploy to the NEW app `herald-scraper-bot-test`, prod untouched — tasks 2-4.
D-06: Fly managed Postgres via `fly mpg` vs legacy `fly postgres` — task 1 decision checkpoint + task 2 provision.
D-07: bot connects to the test Discord server "the herald replays server" — task 4 live pong.
D-08: done bar deploy + boot + migrate + Discord login + @bot→pong — this plan's must_have truths, tasks 3-4.
D-10: new MA-beta Anthropic key as a deploy-time Fly secret — task 2 secrets.
D-11: all secrets from Fly secrets/env, none in source — task 2 secrets.
</decisions_covered>

<objective>
Take the buildable fork live on a NEW, prod-isolated Fly app `herald-scraper-bot-test`: create
the app, provision + attach Fly Postgres, set the app's secrets (with the required
`postgres://` → `postgresql+asyncpg://` scheme rewrite), deploy, confirm Alembic migrations run
on release, and prove the full round-trip with a live `@bot → pong` in the test Discord server.
This is the Walking Skeleton's acceptance bar (D-08).

Purpose: Completes PLAT-04 (deploy on Fly with Postgres attached + migrations on release) and the
runtime half of PLAT-02 (secrets from Fly secrets only). Proves the end-to-end loop before Phase 2
builds real behavior on it.

Output: A running `herald-scraper-bot-test` deployment, a validated `fly.toml`, and a confirmed
live pong reply — with prod `herald-scraper-bot` untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-clean-platform-skeleton/01-CONTEXT.md
@.planning/phases/01-clean-platform-skeleton/01-RESEARCH.md
@.planning/phases/01-clean-platform-skeleton/01-02-SUMMARY.md

<deploy_reference>
- Fly CLI is installed (flyctl v0.4.60). Region: `sea`. App: `herald-scraper-bot-test` (NEW —
  never `herald-scraper-bot`, which is PROD and must stay on its current release, D-03/D-05).
- Postgres decision (D-06): RESEARCH flags that D-06's literal `fly postgres create` is the
  deprecated unmanaged path and Fly now steers to `fly mpg` (Managed Postgres). Task 1 opens a
  decision checkpoint to confirm `fly mpg` vs legacy `fly postgres create` before provisioning.
- DATABASE_URL scheme rewrite (Pitfall 2, REQUIRED): `fly ... attach` sets `DATABASE_URL=postgres://...`.
  The app + Alembic need `DAIMON_DATABASE__URL=postgresql+asyncpg://...`. Set a SECOND Fly secret
  `DAIMON_DATABASE__URL` with the scheme rewritten (sed `s#^postgres://#postgresql+asyncpg://#`).
- App secrets (all via `fly secrets set -a herald-scraper-bot-test`, never in source):
  DAIMON_DATABASE__URL (rewritten), DAIMON_DISCORD__BOT_TOKEN (test-server bot), DAIMON_ANTHROPIC__API_KEY
  (new MA-beta key the user provides, D-10). DAIMON_LOG__LEVEL is set in fly.toml [env].
- Release migrations (Pattern 3): fly.toml `release_command = "alembic upgrade head"` runs on the
  new image and must exit 0 before traffic swaps. Release VM inherits the app's secrets (A3) —
  DAIMON_DATABASE__URL must be spelled exactly.
- Test Discord server: "the herald replays server" (D-07). Test-server guild/channel where the bot
  is already installed. The bot must connect there, NOT any prod channel.
</deploy_reference>

<interfaces>
From plan 02 (already built + locally verified):
  fly.toml — app herald-scraper-bot-test, discord process, release_command alembic upgrade head, discord_liveness check
  Dockerfile — multi-stage uv build, no defaults dir
  docker/entrypoint.sh — exec "$@" (no defaults apply)
  packages/adapters/discord — HeraldBot mention→pong, __main__ starts liveness responder on health_port 8081
  packages/core/alembic/versions/0001_baseline.py — empty baseline migration
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm Postgres product (fly mpg vs legacy fly postgres) + secret prerequisites</name>
  <decision>Which Fly Postgres product to provision for herald-scraper-bot-test.</decision>
  <context>
    D-06 literally names `fly postgres create` (unmanaged), but RESEARCH Pitfall 3 documents that
    Fly now steers new deploys to `fly mpg` (Managed Postgres) and "cannot provide support or
    guidance for unmanaged Postgres." Both produce a `postgres://`-shaped DATABASE_URL and both
    work today; the choice affects long-term support, not the Phase 1 boot bar. Also confirm the
    two user-supplied secrets are ready: the test-server DISCORD_BOT_TOKEN and the new MA-beta
    ANTHROPIC_API_KEY (D-10).
  </context>
  <options>
    <option id="fly-mpg">
      <name>Fly Managed Postgres (`fly mpg create` + `fly mpg attach`)</name>
      <pros>Fly's currently-supported product; HA/backups/PGBouncer pooling; safer long-term bet for a fresh 2026 deploy</pros>
      <cons>Diverges from D-06's literal wording (must reconcile with the user)</cons>
    </option>
    <option id="fly-postgres-legacy">
      <name>Legacy unmanaged (`fly postgres create` + `fly postgres attach`)</name>
      <pros>Matches D-06's literal command; familiar</pros>
      <cons>Fly won't support it going forward; deprecated in spirit</cons>
    </option>
  </options>
  <resume-signal>Select: fly-mpg or fly-postgres-legacy. Also confirm DISCORD_BOT_TOKEN (test server) and ANTHROPIC_API_KEY (MA beta) are ready to set as Fly secrets.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create the test Fly app, provision + attach Postgres, set secrets with scheme rewrite</name>
  <read_first>
    - .planning/phases/01-clean-platform-skeleton/01-RESEARCH.md (Code Examples: "Fly Postgres attach + DATABASE_URL scheme fix"; Pitfall 2, Pitfall 4)
    - fly.toml (from plan 02 — confirms app name herald-scraper-bot-test)
    - .planning/phases/01-clean-platform-skeleton/01-CONTEXT.md (D-05, D-06, D-11)
  </read_first>
  <action>
    D-05: create the NEW test app (prod herald-scraper-bot must stay untouched) WITHOUT deploying
    yet: `fly apps create herald-scraper-bot-test` (region intent `sea`). D-06: provision Postgres
    using the product chosen in task 1 — either `fly mpg create --region sea` (+ `fly mpg attach
    <clusterID> -a herald-scraper-bot-test`) or `fly postgres create --name
    herald-scraper-bot-test-db --region sea` (+ `fly postgres attach herald-scraper-bot-test-db -a
    herald-scraper-bot-test`). Attach sets `DATABASE_URL=postgres://...`
    on the app. Read that value and set the app's REAL config secret with the scheme rewritten:
    `fly secrets set -a herald-scraper-bot-test DAIMON_DATABASE__URL="$(...| sed 's#^postgres://#postgresql+asyncpg://#')"`
    (Pitfall 2 — this is REQUIRED; the app never rewrites schemes itself). Set the two
    user-supplied secrets: `fly secrets set -a herald-scraper-bot-test
    DAIMON_DISCORD__BOT_TOKEN=<test-server bot token>` and
    `DAIMON_ANTHROPIC__API_KEY=<new MA-beta key>` (D-10 new MA-beta key as a deploy-time Fly secret;
    D-11 all secrets from Fly secrets, none in source). Do NOT put any of these values in
    source, fly.toml, or logs. Confirm the secret names via `fly secrets list -a
    herald-scraper-bot-test` (names only — values are never displayed). If `fly` returns an auth
    error at any step, that surfaces a dynamic auth gate — pause and let the user `fly auth login`,
    then retry. Verify prod is untouched: `fly status -a herald-scraper-bot` still shows its
    current running release (do NOT modify it).
  </action>
  <verify>
    <automated>fly secrets list -a herald-scraper-bot-test 2>/dev/null | grep -q DAIMON_DATABASE__URL && fly secrets list -a herald-scraper-bot-test 2>/dev/null | grep -q DAIMON_DISCORD__BOT_TOKEN && fly secrets list -a herald-scraper-bot-test 2>/dev/null | grep -q DAIMON_ANTHROPIC__API_KEY && echo OK</automated>
  </verify>
  <done>herald-scraper-bot-test app exists; Postgres attached; DAIMON_DATABASE__URL (asyncpg scheme), DAIMON_DISCORD__BOT_TOKEN, DAIMON_ANTHROPIC__API_KEY set as Fly secrets; prod herald-scraper-bot unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Deploy to herald-scraper-bot-test and confirm release migrations ran</name>
  <read_first>
    - .planning/phases/01-clean-platform-skeleton/01-RESEARCH.md (Pattern 3: release_command; Pitfall 4: DAIMON_DATABASE__URL spelling in release VM; A3)
    - fly.toml (from plan 02)
    - Dockerfile, docker/entrypoint.sh (from plan 02)
  </read_first>
  <action>
    Deploy: `fly deploy -a herald-scraper-bot-test` (use `--remote-only` if local Docker is
    unavailable). Fly builds the image, then runs the release VM `alembic upgrade head` (through
    the simplified entrypoint) against the attached Postgres — it must exit 0 before traffic swaps.
    Inspect the release logs (`fly logs -a herald-scraper-bot-test` and the deploy output's release
    phase) and CONFIRM the migration step ran successfully (look for the `0001_baseline` /
    `alembic upgrade head` completion, NOT a `RuntimeError: No database URL configured` — if that
    error appears, the DAIMON_DATABASE__URL secret name is misspelled per Pitfall 4). Confirm the
    `discord` process VM reaches a healthy state (the discord_liveness http check on port 8081
    passes) and the logs show `starting_discord_bot` / `discord_ready` without a
    `daimon defaults apply` crash. Do NOT touch the prod app during deploy.
  </action>
  <verify>
    <automated>fly status -a herald-scraper-bot-test 2>/dev/null | grep -iq -E "running|passing|healthy" && echo OK</automated>
  </verify>
  <done>Deploy succeeds; release_command ran `alembic upgrade head` cleanly (baseline applied); discord process VM is running and passing its liveness check; no defaults-apply or missing-DB-URL error.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live @bot → pong round-trip in the test Discord server</name>
  <what-built>
    The bot is deployed to `herald-scraper-bot-test`, connected to Postgres with baseline
    migrations applied, and logged into the test Discord server ("the herald replays server", D-07).
    This is the Walking Skeleton acceptance bar (D-08): prove the full loop deploy → boot →
    Discord login → receive mention → reply, end-to-end, on a prod-isolated app.
  </what-built>
  <how-to-verify>
    1. In the TEST Discord server ("the herald replays server", D-07) — NOT any prod channel — post a
       message that `@mentions` the bot (e.g. `@herald-scraper-bot-test hello`).
    2. Expect the bot to reply `pong` in the same channel within a couple seconds.
    3. Post a message in the same channel that does NOT mention the bot. Expect NO reply
       (confirms DISC-01 "never posts unprompted").
    4. Optionally check `fly logs -a herald-scraper-bot-test` shows the mention handled without errors.
    5. Confirm prod `herald-scraper-bot` was never redeployed (its Fly status shows its prior release).
  </how-to-verify>
  <resume-signal>Type "approved" if the bot replied `pong` to a mention and stayed silent otherwise, or describe what happened.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fly platform → app | Secrets injected at runtime; must never appear in source or image |
| attached Postgres → app | Connection string is a secret; scheme must be rewritten for asyncpg |
| deploy operation → prod app | Deploy must target the test app only, never prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | DATABASE_URL / bot token / anthropic key | mitigate | All set via `fly secrets set` (never source/fly.toml/logs); verified by `fly secrets list` names-only; SecretStr in config prevents log leakage |
| T-03-02 | Tampering | Deploying over prod herald-scraper-bot | mitigate | Every fly command pins `-a herald-scraper-bot-test`; task 2/3 explicitly verify prod status unchanged (D-03/D-05) |
| T-03-03 | Denial of Service | Release migration fails on misspelled DB URL secret | mitigate | Pitfall 4 guard — confirm DAIMON_DATABASE__URL exact spelling; release logs inspected for RuntimeError before accepting deploy |
| T-03-04 | Spoofing / Info Disclosure | Bot connecting to a prod Discord channel | mitigate | Test-server bot token only (D-07); human-verify checkpoint confirms reply landed in the test server, not prod |
</threat_model>

<verification>
- `fly secrets list -a herald-scraper-bot-test` shows DAIMON_DATABASE__URL, DAIMON_DISCORD__BOT_TOKEN, DAIMON_ANTHROPIC__API_KEY (names only)
- `fly deploy` release phase logs show `alembic upgrade head` completing (baseline applied), no missing-DB-URL error
- `fly status -a herald-scraper-bot-test` shows discord process running + liveness check passing
- Live: `@mention` → `pong` in the test server; no reply when un-mentioned (human-verify)
- `fly status -a herald-scraper-bot` (prod) shows its prior release, untouched
</verification>

<success_criteria>
- herald-scraper-bot-test app deployed with Postgres attached (PLAT-04)
- Alembic migrations ran automatically on release (PLAT-04)
- All secrets from Fly secrets, none in source (PLAT-02)
- Live @bot → pong works in the test server; bot silent otherwise (D-08 acceptance)
- Prod herald-scraper-bot never touched (D-03/D-05)
</success_criteria>

<output>
Create `.planning/phases/01-clean-platform-skeleton/01-03-SUMMARY.md` when done
</output>
</content>
