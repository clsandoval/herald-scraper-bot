---
phase: 01-clean-platform-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bot.py
  - lambda_function.py
  - database.py
  - discord_bot.py
  - functions.py
  - constants.py
  - ability_ids.json
  - requirements.txt
  - text.out
  - download.html
  - pyproject.toml
  - alembic.ini
  - packages/core/pyproject.toml
  - packages/core/daimon/core/__init__.py
  - packages/core/daimon/core/db.py
  - packages/core/daimon/core/_models.py
  - packages/core/daimon/core/config.py
  - packages/core/daimon/core/health.py
  - packages/core/daimon/core/logging_setup.py
  - packages/core/daimon/core/ids.py
  - packages/core/daimon/core/errors.py
  - packages/core/daimon/core/ma.py
  - packages/core/daimon/core/turn/__init__.py
  - packages/core/daimon/core/turn/driver.py
  - packages/core/daimon/core/turn/gating.py
  - packages/core/daimon/core/turn/lifecycle.py
  - packages/core/daimon/core/turn/reducers.py
  - packages/core/daimon/core/turn/render.py
  - packages/core/daimon/core/turn/state.py
  - packages/core/daimon/core/skills/__init__.py
  - packages/core/daimon/core/skills/fetch.py
  - packages/core/daimon/core/py.typed
  - packages/core/alembic/env.py
  - packages/core/alembic/script.py.mako
  - tests/test_config.py
autonomous: false
requirements: [PLAT-01, PLAT-02, PLAT-03]
must_haves:
  truths:
    - "D-01: The trimmed daimon subset is copied INTO this existing repo (not a fresh repo) — legacy Telegram/Lambda/dead files (bot.py, lambda_function.py, database.py, discord_bot.py, functions.py, constants.py) are gone from the working tree"
    - "D-02: The leaked Telegram token no longer appears anywhere in git history (git filter-repo scrub + one-time force-push)"
    - "A uv workspace root plus a trimmed daimon-core package exist and import cleanly"
    - "D-11: Config, db, health, logging, models load from env only — no secret literals in source (SecretStr on tokens)"
    - "The turn/ engine and skills/ package are copied INERT (present, importable, unused in Phase 1) — daimon.core.turn and daimon.core.skills import cleanly"
  artifacts:
    - path: "pyproject.toml"
      provides: "uv workspace root declaring packages/core (+ discord/scheduler members)"
      contains: "tool.uv.workspace"
    - path: "packages/core/daimon/core/config.py"
      provides: "Trimmed pydantic-settings Settings (database, anthropic, discord, log only)"
      contains: "env_nested_delimiter"
    - path: "packages/core/daimon/core/db.py"
      provides: "build_engine / build_session_factory (copied verbatim)"
      contains: "create_async_engine"
    - path: "packages/core/daimon/core/_models.py"
      provides: "Empty declarative Base — no domain tables in Phase 1"
      contains: "class Base(DeclarativeBase)"
    - path: "packages/core/daimon/core/turn/driver.py"
      provides: "MA turn driver (run_turn) copied verbatim — inert in Phase 1, wired in Phase 2 (PLAT-01, D-04, D-09)"
      contains: "async def run_turn"
    - path: "packages/core/daimon/core/turn/state.py"
      provides: "TurnState + content blocks copied verbatim (inert)"
      contains: "class TurnState"
    - path: "packages/core/daimon/core/turn/gating.py"
      provides: "Pure should_admit_turn concurrency gate copied verbatim (inert)"
      contains: "def should_admit_turn"
    - path: "packages/core/daimon/core/ma.py"
      provides: "Anthropic Managed Agents wrapper copied verbatim — kept per D-09, dependency of turn/ (inert)"
      contains: "replay_events"
    - path: "packages/core/daimon/core/skills/fetch.py"
      provides: "GitHub skill-repo fetch copied verbatim — clean skills subset (inert), no defaults/ deps"
      contains: "async def fetch_repo"
    - path: "packages/core/daimon/core/errors.py"
      provides: "Full daimon error taxonomy copied verbatim (TurnError/DefaultsError needed by turn/, ma, skills)"
      contains: "class TurnError"
    - path: "packages/core/alembic/env.py"
      provides: "Async Alembic env reading DAIMON_DATABASE__URL (copied verbatim)"
      contains: "async_engine_from_config"
    - path: "tests/test_config.py"
      provides: "Import-smoke + config-from-env test, no hardcoded secrets"
  key_links:
    - from: "packages/core/alembic/env.py"
      to: "packages/core/daimon/core/_models.py"
      via: "from daimon.core._models import Base"
      pattern: "from daimon.core._models import Base"
    - from: "packages/core/daimon/core/config.py"
      to: "environment variables"
      via: "pydantic-settings env_prefix DAIMON_"
      pattern: "env_prefix"
    - from: "packages/core/daimon/core/turn/driver.py"
      to: "packages/core/daimon/core/ma.py"
      via: "from daimon.core.ma import replay_events, send_interrupt_and_wait, terminal_stop_reason"
      pattern: "from daimon.core.ma import"
---

<decisions_covered>
D-01: copy trimmed daimon into THIS repo, not a fresh repo — task 1 git-rm's legacy files in-place.
D-02: scrub the leaked Telegram token from git history with git filter-repo + one-time force-push — tasks 3-4.
D-04: keep only the trimmed core, and D-09: keep the Anthropic Managed Agents turn/ + ma.py inert — task 1.
D-11: all secrets from env, no secret values in source; SecretStr on tokens — task 2 config.
</decisions_covered>

<objective>
Purge the legacy auto-posting bot (Telegram/Lambda/Discord/dead code) from the working tree
AND from git history (scrubbing the leaked Telegram token), then scaffold the trimmed daimon
fork's `uv` workspace root and `daimon-core` package — including the `turn/` MA engine and the
clean `skills/` subset copied INERT (present but unused in Phase 1, wired in Phase 2). This is
the foundation layer: after this plan the repo is secret-clean and importable, but does not yet
boot a bot or deploy.

Purpose: Satisfies PLAT-01 (keep only the trimmed core — which per D-04 and Phase-1
success-criterion #1 explicitly INCLUDES `turn/` and `skills/`), PLAT-02 (secrets from env only),
and PLAT-03 (leaked token revoked-and-scrubbed, legacy dead code removed). Keeping the Anthropic
Managed Agents `turn/` + `ma.py` machinery is locked by D-09 ("keep Anthropic Managed Agents, do
NOT rip it out"). Establishes the package skeleton every later plan builds on.

Output: A clean working tree with `pyproject.toml` (uv workspace root), a `packages/core`
daimon-core package (db/config/health/logging/ids/errors/empty-models PLUS inert turn/ + ma.py +
clean skills/), `alembic.ini` + async `alembic/env.py`, a scrubbed git history, and an
import-smoke test that includes `daimon.core.turn` and `daimon.core.skills`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-clean-platform-skeleton/01-CONTEXT.md
@.planning/phases/01-clean-platform-skeleton/01-RESEARCH.md

<fork_source_files>
Copy these verbatim (or near-verbatim) from the daimon fork:
- /home/clsandoval/cs/daimon-cma-open-source/packages/core/daimon/core/db.py — copy AS-IS
- /home/clsandoval/cs/daimon-cma-open-source/packages/core/daimon/core/health.py — copy AS-IS
- /home/clsandoval/cs/daimon-cma-open-source/packages/core/daimon/core/logging_setup.py — copy AS-IS
- /home/clsandoval/cs/daimon-cma-open-source/packages/core/daimon/core/ids.py — copy AS-IS
- /home/clsandoval/cs/daimon-cma-open-source/packages/core/daimon/core/errors.py — copy AS-IS (do NOT reduce to DaimonError-only; TurnError/DefaultsError/SpecError are hard deps of turn/, ma.py, and skills/fetch.py)
- /home/clsandoval/cs/daimon-cma-open-source/packages/core/daimon/core/ma.py — copy AS-IS (kept per D-09; dependency of turn/driver.py; deps only errors + anthropic SDK + structlog)
- /home/clsandoval/cs/daimon-cma-open-source/packages/core/daimon/core/turn/ (whole dir: __init__.py, driver.py, gating.py, lifecycle.py, reducers.py, render.py, state.py) — copy AS-IS, INERT
- /home/clsandoval/cs/daimon-cma-open-source/packages/core/daimon/core/skills/__init__.py — copy AS-IS (empty)
- /home/clsandoval/cs/daimon-cma-open-source/packages/core/daimon/core/skills/fetch.py — copy AS-IS (clean subset; deps only errors + httpx + structlog)
- /home/clsandoval/cs/daimon-cma-open-source/packages/core/alembic/env.py — copy AS-IS
- /home/clsandoval/cs/daimon-cma-open-source/packages/core/daimon/core/config.py — TRIM (see task 2)
- /home/clsandoval/cs/daimon-cma-open-source/packages/core/pyproject.toml — TRIM deps to what core actually needs
- /home/clsandoval/cs/daimon-cma-open-source/alembic.ini — copy AS-IS (script_location = packages/core/alembic)

DO NOT copy (hard deps on EXCLUDED machinery — same exclusions plan 01-02's runtime.py applies):
- skills/discover.py, skills/sync.py, skills/pipeline.py (import daimon.core.defaults.*, skill_zip, specs — the defaults/ tree, billing/provisioning/MCP hooks are excluded per D-04)
- ma_resolver.py, defaults/, billing.py, provisioning, MCP modules
</fork_source_files>

<interfaces>
From daimon core/db.py (copy verbatim, no changes):
  def build_engine(url: str, *, echo: bool = False) -> AsyncEngine
  def build_session_factory(engine: AsyncEngine) -> async_sessionmaker[AsyncSession]

From daimon core/config.py (the nested pydantic-settings shape to preserve):
  class DatabaseSettings(BaseModel): url: PostgresDsn; test_url: PostgresDsn | None = None
  class AnthropicSettings(BaseModel): api_key: SecretStr; base_url: HttpUrl = "https://api.anthropic.com"
  class DiscordSettings(BaseModel): bot_token: SecretStr; health_port: int = 8081
  class LogSettings(BaseModel): level: Literal["DEBUG","INFO","WARNING","ERROR"] = "INFO"
  class Settings(BaseSettings): model_config = SettingsConfigDict(env_prefix="DAIMON_", env_nested_delimiter="__", env_file=".env", extra="ignore")
  def load_settings() -> Settings   # construct-at-edge, NO module-level singleton

From daimon core/turn/ (copied verbatim, INERT — no Phase-1 caller):
  turn/driver.py:  async def run_turn(...)   # imports daimon.core.ma + daimon.core.errors.TurnError + internal turn.* + tenacity
  turn/gating.py:  def should_admit_turn(*, current_in_flight: int, cap: int) -> bool   # pure, no deps
  turn/state.py:   class TurnState / ContentBlock / TextBlock / ToolUseBlock
  turn/ deps that MUST also be present: daimon.core.ma (copied), daimon.core.errors (copied verbatim)

From daimon core/ma.py (copied verbatim, INERT):
  replay_events, send_interrupt_and_wait, terminal_stop_reason   # deps: errors + anthropic SDK + structlog only

From daimon alembic/env.py (copy verbatim):
  reads os.environ["DAIMON_DATABASE_URL"] or ["DAIMON_DATABASE__URL"]
  target_metadata = Base.metadata   # imported from daimon.core._models
</interfaces>

Leaked token to purge: the string `REDACTED`
lives at functions.py:94 in the current working tree AND in git history.
Current repo has: single `origin` remote (github.com/clsandoval/herald-scraper-bot.git),
single `main` branch, 18 commits, no CI/CD git webhook (safe to force-push per D-02/D-03).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete legacy code and scaffold the uv workspace + daimon-core package (incl. inert turn/ + skills/)</name>
  <read_first>
    - /home/clsandoval/cs/herald-scraper-bot/ working tree (bot.py, lambda_function.py, database.py, discord_bot.py, functions.py, constants.py, ability_ids.json, requirements.txt, text.out, download.html)
    - /home/clsandoval/cs/daimon-cma-open-source/packages/core/daimon/core/db.py
    - /home/clsandoval/cs/daimon-cma-open-source/packages/core/daimon/core/health.py
    - /home/clsandoval/cs/daimon-cma-open-source/packages/core/daimon/core/logging_setup.py
    - /home/clsandoval/cs/daimon-cma-open-source/packages/core/daimon/core/ids.py
    - /home/clsandoval/cs/daimon-cma-open-source/packages/core/daimon/core/errors.py
    - /home/clsandoval/cs/daimon-cma-open-source/packages/core/daimon/core/ma.py (imports: errors + anthropic + structlog only)
    - /home/clsandoval/cs/daimon-cma-open-source/packages/core/daimon/core/turn/ (all files; note driver.py imports daimon.core.ma + tenacity)
    - /home/clsandoval/cs/daimon-cma-open-source/packages/core/daimon/core/skills/__init__.py and skills/fetch.py (fetch.py imports: errors + httpx + structlog only)
    - /home/clsandoval/cs/daimon-cma-open-source/packages/core/alembic/env.py
    - /home/clsandoval/cs/daimon-cma-open-source/alembic.ini
    - /home/clsandoval/cs/daimon-cma-open-source/packages/core/pyproject.toml
    - /home/clsandoval/cs/daimon-cma-open-source/pyproject.toml (workspace root shape)
  </read_first>
  <action>
    D-01: copy the trimmed daimon subset INTO this existing repo (not a fresh repo) — replace
    contents in-place. Delete the legacy files from the working tree with `git rm`: bot.py,
    lambda_function.py, database.py, discord_bot.py, functions.py, constants.py, ability_ids.json,
    requirements.txt, text.out, download.html (per PLAT-01/PLAT-03 — keep LICENSE, CLAUDE.md,
    .gitignore, .planning/).
    Then scaffold the uv workspace: create `pyproject.toml` at repo root declaring
    `[tool.uv.workspace]` members `["packages/core", "packages/adapters/*"]` and
    `[tool.uv.sources]` mapping daimon-core (and the discord/scheduler adapters, added in plan 02)
    as `{ workspace = true }`; carry over daimon's `[tool.ruff]` (line-length 100, target py312)
    but drop the importlinter/pyright multi-adapter contracts that reference stripped packages.
    Create `packages/core/pyproject.toml` (name `daimon-core`, requires-python >=3.12) with a
    TRIMMED dependency list — keep sqlalchemy[asyncio]>=2.0, asyncpg>=0.29, alembic>=1.13,
    pydantic>=2.8, pydantic-settings>=2.5, structlog>=24.1, anthropic>=0.96 (kept per D-09), plus
    tenacity>=8 (imported by turn/driver.py) and httpx>=0.27 (imported by skills/fetch.py and the
    anthropic client); drop google-auth, PyJWT, cachetools, httpx-sse, pyyaml unless a copied core
    file imports them (verify by import). Copy verbatim into `packages/core/daimon/core/`: db.py,
    health.py, logging_setup.py, ids.py, and errors.py copied AS-IS in full (do NOT reduce to a
    DaimonError-only stub — TurnError, DefaultsError and SpecError are hard dependencies of the
    turn/, ma.py, and skills/fetch.py modules copied below). Copy ma.py verbatim (kept per D-09;
    it is the sole non-turn daimon dependency of turn/driver.py). Copy the ENTIRE turn/ directory
    verbatim into `packages/core/daimon/core/turn/` — __init__.py, driver.py, gating.py,
    lifecycle.py, reducers.py, render.py, state.py — as an INERT engine (present and importable but
    with NO Phase-1 caller; Phase 2 wires it, mirroring the accepted "scheduler copied inert"
    pattern in plan 02). Copy the CLEAN skills subset into `packages/core/daimon/core/skills/`:
    __init__.py (empty) and fetch.py ONLY. Do NOT copy skills/discover.py, skills/sync.py, or
    skills/pipeline.py — they import daimon.core.defaults.*, skill_zip, and specs (EXCLUDED
    machinery per D-04, the same exclusions plan 02's runtime.py applies). Create an empty
    `packages/core/daimon/core/__init__.py` and `py.typed`. Copy `alembic.ini` to repo root AS-IS
    and `alembic/env.py` to `packages/core/alembic/env.py` AS-IS, plus a standard Alembic
    `script.py.mako` (from daimon's alembic dir or `alembic init` default). Do NOT create a
    baseline migration here — that is plan 02's job after _models.py/config.py exist.
    Create the empty `packages/core/alembic/versions/` directory with a `.gitkeep`.
  </action>
  <verify>
    <automated>cd /home/clsandoval/cs/herald-scraper-bot && test ! -f functions.py && test ! -f bot.py && test ! -f lambda_function.py && test ! -f database.py && test -f pyproject.toml && test -f packages/core/daimon/core/db.py && test -f packages/core/daimon/core/ma.py && test -f packages/core/daimon/core/turn/driver.py && test -f packages/core/daimon/core/turn/gating.py && test -f packages/core/daimon/core/skills/fetch.py && test ! -f packages/core/daimon/core/skills/sync.py && test ! -f packages/core/daimon/core/skills/discover.py && test -f packages/core/alembic/env.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Legacy files (functions.py, bot.py, lambda_function.py, database.py, discord_bot.py, constants.py, ability_ids.json, requirements.txt, text.out, download.html) removed from the working tree via `git rm`.
    - uv workspace root pyproject.toml + daimon-core package skeleton present: db/health/logging/ids/errors (errors.py full, not stubbed) + alembic.ini/env.py + script.py.mako.
    - turn/ copied verbatim in full (all 7 files) and ma.py copied verbatim — both INERT (no Phase-1 caller).
    - skills/ contains ONLY __init__.py + fetch.py; discover.py/sync.py/pipeline.py NOT copied (excluded-machinery deps).
    - packages/core/pyproject.toml deps include tenacity + httpx (needed by turn/ + skills/fetch); no baseline migration yet.
  </acceptance_criteria>
  <done>Legacy files removed from working tree; uv workspace root + daimon-core package skeleton (db/health/logging/ids/errors + full turn/ + ma.py + clean skills/ + alembic.ini/env.py) present; skills/ excluded-machinery submodules NOT copied; no baseline migration yet.</done>
</task>

<task type="auto">
  <name>Task 2: Trim config.py to Phase-1 sections + empty models Base, verify uv sync + import smoke (incl. turn/ + skills/)</name>
  <read_first>
    - /home/clsandoval/cs/daimon-cma-open-source/packages/core/daimon/core/config.py (full — note all nested Settings sections)
    - /home/clsandoval/cs/daimon-cma-open-source/packages/core/daimon/core/_models.py (first 40 lines — the Base declaration)
    - packages/core/daimon/core/db.py (just written — for the AsyncSession type it produces)
    - packages/core/daimon/core/turn/__init__.py (just written — the re-exports that must import cleanly)
    - .planning/phases/01-clean-platform-skeleton/01-RESEARCH.md (Pattern 1: pydantic-settings; "Trim guidance" note)
  </read_first>
  <action>
    Write `packages/core/daimon/core/config.py` keeping ONLY these nested sections from daimon:
    `DatabaseSettings` (url: PostgresDsn, test_url optional), `AnthropicSettings`
    (api_key: SecretStr, base_url default), `DiscordSettings` (bot_token: SecretStr,
    health_port: int = 8081 — drop max_concurrent_turns_per_tenant / per_caller_thread_sessions
    which are Phase 6+), and `LogSettings` (level literal). Drop McpSettings, SlackSettings,
    CLISettings, GithubSettings, CryptoSettings, CredentialsSettings, GeminiSettings,
    NotebookSettings, BillingSettings, and any `defaults_root`. The `Settings(BaseSettings)`
    class keeps `database`, `anthropic`, `discord` (optional), `log`, with
    `model_config = SettingsConfigDict(env_prefix="DAIMON_", env_nested_delimiter="__",
    env_file=".env", env_file_encoding="utf-8", extra="ignore")`. Preserve `load_settings()`
    as a construct-at-edge function (no module-level singleton). D-11: all secrets come from env
    only, never source literals — keep `SecretStr` on bot_token and api_key (do NOT downgrade to
    plain str; prevents log/repr leakage, per threat T-01-02).
    Write `packages/core/daimon/core/_models.py` as an EMPTY schema: module docstring +
    `from sqlalchemy.orm import DeclarativeBase` + `class Base(DeclarativeBase): ...` and nothing
    else (no domain tables — Phase 1 has zero DATA-* requirements). Run `uv sync --no-dev` from
    repo root to resolve the workspace and generate `uv.lock`. Confirm the core package imports —
    INCLUDING the inert engine: `daimon.core.turn` and `daimon.core.skills` MUST import cleanly
    (this proves the turn/ + skills/ copy is self-consistent and its deps — ma.py, errors.py,
    tenacity, httpx — are all present). Write `tests/test_config.py` asserting: (a)
    `from daimon.core.config import Settings, load_settings` imports; (b) `Settings` constructed
    from patched env (DAIMON_DATABASE__URL, DAIMON_ANTHROPIC__API_KEY, DAIMON_DISCORD__BOT_TOKEN)
    reads those values and exposes bot_token as SecretStr (repr does not contain the raw value);
    (c) `from daimon.core._models import Base` imports and `Base.metadata.tables` is empty;
    (d) `import daimon.core.turn` and `import daimon.core.skills` both import without error
    (inert-engine smoke).
  </action>
  <verify>
    <automated>cd /home/clsandoval/cs/herald-scraper-bot && uv sync --no-dev && uv run python -c "from daimon.core.config import Settings, load_settings; from daimon.core._models import Base; import daimon.core.turn; import daimon.core.skills; from daimon.core.turn import gating; assert len(Base.metadata.tables)==0, Base.metadata.tables; print('OK')" && uv run pytest tests/test_config.py -q</automated>
  </verify>
  <acceptance_criteria>
    - config.py exposes only database/anthropic/discord/log sections; bot_token + api_key stay SecretStr.
    - _models.py Base has zero tables; `uv sync --no-dev` resolves and produces uv.lock.
    - `import daimon.core.turn` and `import daimon.core.skills` (and `daimon.core.turn.gating`) import cleanly — inert engine is self-consistent.
    - tests/test_config.py passes (import smoke + config-from-env + SecretStr repr + empty Base + turn/skills import).
  </acceptance_criteria>
  <done>config.py exposes only database/anthropic/discord/log sections with SecretStr tokens; _models.py Base has zero tables; uv sync resolves; import smoke (including daimon.core.turn + daimon.core.skills) + config test pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Verify git-filter-repo legitimacy + confirm token revocation</name>
  <what-built>
    A local `git-filter-repo` install is required to scrub the leaked Telegram token from git
    history (D-02). `git-filter-repo` is NOT installed on this machine. Per the RESEARCH Package
    Legitimacy Audit, this dev-only tool was NOT run through slopcheck mechanically this session,
    so its install is gated for human confirmation before proceeding (threat T-01-SC).
    The intended install is `pip install git-filter-repo` (dev machine only — NOT added to any
    package's dependencies and NOT shipped in the Docker image).
  </what-built>
  <how-to-verify>
    1. Confirm the package is legitimate at https://pypi.org/project/git-filter-repo/ — expect
       a maintained project (author Elijah Newren, github.com/newren/git-filter-repo, 2019+,
       endorsed by GitHub's own "Removing sensitive data" docs).
    2. Approve the local dev-only install `pip install git-filter-repo` (or `pipx install git-filter-repo`).
    3. Confirm you have ALREADY revoked the leaked Telegram token via @BotFather (D-11 user
       action) — the scrub is defense-in-depth; revocation is the real fix. If not yet revoked,
       do it now before approving.
  </how-to-verify>
  <resume-signal>Type "approved" once the package is verified and revocation is done, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Install git-filter-repo, scrub functions.py from history, force-push</name>
  <read_first>
    - .planning/phases/01-clean-platform-skeleton/01-RESEARCH.md (Code Examples: "git filter-repo — purge the leaked Telegram token"; Pitfall 5: force-push consequences)
    - .planning/phases/01-clean-platform-skeleton/01-CONTEXT.md (D-02, D-03)
  </read_first>
  <action>
    D-02: scrub the leaked Telegram token from git history with git filter-repo + a one-time
    force-push. Install the approved tool: `pip install git-filter-repo`. From the repo root run
    `git filter-repo --path functions.py --invert-paths --force` to purge every historical blob
    of functions.py (the file carrying the leaked token at functions.py:94) from all history.
    `git-filter-repo` removes the `origin` remote as a safety measure — re-add it with
    `git remote add origin https://github.com/clsandoval/herald-scraper-bot.git`. Then perform
    the one-time force-push (D-02): `git push origin --force --all` and
    `git push origin --force --tags`. This rewrites all commit SHAs; safe here because prod runs
    off its Fly machine (not the repo) and there is no CI/CD git webhook (D-03). Do NOT delete
    other files here — task 1 already git-rm'd them; filter-repo only targets git HISTORY for
    functions.py specifically because it held the secret.
  </action>
  <verify>
    <automated>cd /home/clsandoval/cs/herald-scraper-bot && test "$(git log --all -p 2>/dev/null | grep -c 'REDACTED')" = "0" && test "$(git log --all --oneline -- functions.py | wc -l)" = "0" && echo OK</automated>
  </verify>
  <done>functions.py absent from all git history; token string returns zero matches across full history; origin re-added and force-pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source repo → public GitHub remote | Committed secrets are exposed to anyone with repo/history access |
| env/Fly secrets → app config | Secrets must enter only via env, never source literals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | Leaked Telegram token in functions.py + git history | mitigate | Revoke via @BotFather (D-11 user action) + `git filter-repo --path functions.py --invert-paths` scrub (task 4); verified by grep-count == 0 across full history |
| T-01-02 | Information Disclosure | Discord/Anthropic tokens via log/repr | mitigate | Keep pydantic `SecretStr` on bot_token/api_key in config.py; test asserts repr does not contain raw value |
| T-01-03 | Tampering (accidental) | Force-push rewrites all SHAs | accept | No CI/CD git webhook exists (verified); prod runs off its Fly machine not the repo (D-03) — low risk, one-time coordinated push |
| T-01-SC | Tampering | `pip install git-filter-repo` (dev-only) | mitigate | Blocking human-verify checkpoint (task 3) confirming pypi.org/project/git-filter-repo legitimacy before install; never auto-approved |
</threat_model>

<verification>
- `functions.py`, `bot.py`, `lambda_function.py`, `database.py`, `discord_bot.py`, `constants.py` absent from working tree
- `git log --all -p | grep -c '1982794836:...'` == 0 (token gone from history)
- `uv sync --no-dev` resolves the workspace and produces `uv.lock`
- `uv run python -c "import daimon.core.config; import daimon.core.db; import daimon.core.turn; import daimon.core.skills"` succeeds (inert turn/ + skills/ import cleanly)
- turn/ present in full (driver/gating/lifecycle/reducers/render/state) + ma.py present; skills/ has only __init__.py + fetch.py (discover/sync/pipeline excluded)
- `uv run pytest tests/test_config.py -q` passes
- `grep -rn "sk-\|xoxb-\|1982794836" packages/ *.py --include="*.py"` returns nothing
</verification>

<success_criteria>
- Legacy Telegram/Lambda/dead files removed (PLAT-01, PLAT-03)
- Leaked token scrubbed from history and revoked (PLAT-03)
- No secret literals in source; config resolves from env (PLAT-02)
- uv workspace + daimon-core package import cleanly with an empty models Base
- turn/ MA engine + ma.py + clean skills/ copied INERT and import cleanly (PLAT-01, D-04, D-09 — Phase-1 success-criterion #1)
</success_criteria>

<output>
Create `.planning/phases/01-clean-platform-skeleton/01-01-SUMMARY.md` when done
</output>
</content>
</invoke>
