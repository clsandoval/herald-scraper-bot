---
phase: 01-clean-platform-skeleton
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/adapters/discord/pyproject.toml
  - packages/adapters/discord/daimon/adapters/discord/__init__.py
  - packages/adapters/discord/daimon/adapters/discord/__main__.py
  - packages/adapters/discord/daimon/adapters/discord/bot.py
  - packages/adapters/discord/daimon/adapters/discord/runtime.py
  - packages/adapters/scheduler/pyproject.toml
  - packages/adapters/scheduler/daimon/adapters/scheduler/__init__.py
  - packages/adapters/scheduler/daimon/adapters/scheduler/main.py
  - packages/adapters/scheduler/daimon/adapters/scheduler/settings.py
  - packages/core/alembic/versions/0001_baseline.py
  - pyproject.toml
  - Dockerfile
  - docker/entrypoint.sh
  - docker-compose.yml
  - fly.toml
  - .env.example
  - packages/adapters/discord/tests/test_mention_pong.py
autonomous: true
requirements: [PLAT-01, PLAT-04]
must_haves:
  truths:
    - "The Discord adapter boots as its own process and replies 'pong' only when @mentioned"
    - "The bot never replies to messages where it is not mentioned (DISC-01 at code level)"
    - "An empty-schema Alembic baseline migration exists and applies cleanly"
    - "The Docker image builds and boots locally against Postgres without calling `daimon defaults apply`"
  artifacts:
    - path: "packages/adapters/discord/daimon/adapters/discord/bot.py"
      provides: "Minimal HeraldBot: on_message → mention check → pong"
      contains: "self.user not in message.mentions"
    - path: "packages/adapters/discord/daimon/adapters/discord/runtime.py"
      provides: "Trimmed DiscordRuntime (settings + anthropic + sessionmaker only)"
      contains: "build_engine"
    - path: "packages/core/alembic/versions/0001_baseline.py"
      provides: "Empty baseline migration (no domain tables)"
      contains: "def upgrade"
    - path: "docker/entrypoint.sh"
      provides: "Simplified entrypoint — no `daimon defaults apply`"
      contains: "exec \"$@\""
    - path: "fly.toml"
      provides: "herald-scraper-bot-test app config, discord process, release_command"
      contains: "herald-scraper-bot-test"
    - path: "packages/adapters/discord/tests/test_mention_pong.py"
      provides: "Unit test: pong sent iff bot mentioned"
  key_links:
    - from: "packages/adapters/discord/daimon/adapters/discord/__main__.py"
      to: "packages/adapters/discord/daimon/adapters/discord/bot.py"
      via: "instantiate HeraldBot with runtime + intents"
      pattern: "HeraldBot"
    - from: "packages/adapters/discord/daimon/adapters/discord/__main__.py"
      to: "packages/core/daimon/core/health.py"
      via: "start_liveness_responder(settings.discord.health_port)"
      pattern: "start_liveness_responder"
    - from: "fly.toml"
      to: "packages/core/alembic"
      via: "release_command = alembic upgrade head"
      pattern: "release_command"
---

<objective>
Build the thinnest Discord adapter that boots and replies `pong` on `@mention`, copy the
scheduler package inert (files only, no Fly process), create the empty-schema Alembic baseline
migration, and adapt Dockerfile/entrypoint/fly.toml/docker-compose for the `herald-scraper-bot-test`
app — dropping daimon's `defaults apply` tenant/MA provisioning. This makes the fork buildable
and locally bootable end-to-end, one deploy away from the live pong.

Purpose: Satisfies PLAT-01 (Discord + scheduler adapters kept, minimal) and the buildable/migrating
half of PLAT-04. The mention-gate also establishes DISC-01 ("never posts unprompted") at the code
level for this phase.

Output: A `packages/adapters/discord` package (bot/runtime/__main__), an inert
`packages/adapters/scheduler` package, an empty baseline migration, a multi-stage Dockerfile, a
simplified entrypoint, a `herald-scraper-bot-test` fly.toml, a local docker-compose, an
`.env.example`, and a mention→pong unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-clean-platform-skeleton/01-CONTEXT.md
@.planning/phases/01-clean-platform-skeleton/01-RESEARCH.md
@.planning/phases/01-clean-platform-skeleton/01-01-SUMMARY.md

<fork_source_files>
- /home/clsandoval/cs/daimon-cma-open-source/packages/adapters/discord/daimon/adapters/discord/__main__.py — ADAPT (drop Sentry/observability init and drop `settings.sentry`; keep the health responder + signal handlers + bot.start)
- /home/clsandoval/cs/daimon-cma-open-source/packages/adapters/discord/daimon/adapters/discord/runtime.py — REWRITE MINIMAL (drop billing_config / notebook_rate_limiter / resolver_cache / deployment_default / defaults loader; keep engine → sessionmaker + AsyncAnthropic)
- /home/clsandoval/cs/daimon-cma-open-source/packages/adapters/discord/daimon/adapters/discord/bot.py — DO NOT TRIM the 1170-line file; write a NEW minimal HeraldBot (see interfaces)
- /home/clsandoval/cs/daimon-cma-open-source/packages/adapters/discord/pyproject.toml — TRIM deps (drop python-ulid unless imported; keep discord.py>=2.7,<3, daimon-core, anthropic, sqlalchemy, asyncpg, structlog)
- /home/clsandoval/cs/daimon-cma-open-source/packages/adapters/scheduler/ (whole tree: pyproject.toml, main.py, settings.py, __init__.py) — COPY package files as-is (inert; NOT wired into fly.toml processes)
- /home/clsandoval/cs/daimon-cma-open-source/Dockerfile — ADAPT (drop `COPY defaults/`, keep multi-stage uv build)
- /home/clsandoval/cs/daimon-cma-open-source/docker/entrypoint.sh — REWRITE to just `set -e; exec "$@"` (drop the `daimon defaults apply` branch)
- /home/clsandoval/cs/daimon-cma-open-source/fly.toml — ADAPT (app=herald-scraper-bot-test, region sea, single discord process + discord_liveness check; drop mcp/scheduler/slack processes+checks+http_service)
- /home/clsandoval/cs/daimon-cma-open-source/docker-compose.yml — ADAPT (postgres + discord only; drop mcp/slack/init-provisioning)
- /home/clsandoval/cs/daimon-cma-open-source/.env.example — ADAPT (document DAIMON_DATABASE__URL, DAIMON_ANTHROPIC__API_KEY, DAIMON_DISCORD__BOT_TOKEN, DAIMON_LOG__LEVEL only; NO secret values)
</fork_source_files>

<interfaces>
From plan 01 (daimon-core, already built):
  daimon.core.config.load_settings() -> Settings   (Settings.discord: DiscordSettings | None, Settings.database.url, Settings.anthropic.api_key)
  daimon.core.db.build_engine(url) / build_session_factory(engine)
  daimon.core.health.start_liveness_responder(port) -> asyncio.Server
  daimon.core.logging_setup.configure_log_level(level)
  daimon.core._models.Base   (empty metadata — baseline migration targets this)

New minimal HeraldBot (bot.py) — follow daimon's on_message dispatch shape, stripped:
  class HeraldBot(discord.Client):
      def __init__(self, *, runtime, intents): super().__init__(intents=intents); self.runtime = runtime
      async def on_ready(self): log.info("discord_ready", user=str(self.user))
      async def on_message(self, message):
          if message.author == self.user: return          # ignore self
          if self.user not in message.mentions: return     # DISC-01: only on mention
          await message.channel.send("pong")
  # message_content intent MUST be enabled in __main__ (intents.message_content = True)
  # Phase 1 does NOT invoke the MA turn driver — runtime.anthropic is wired but unused by pong.

Trimmed DiscordRuntime (runtime.py):
  @dataclass(frozen=True) class DiscordRuntime: settings: Settings; anthropic: AsyncAnthropic; sessionmaker: async_sessionmaker[AsyncSession]
  @asynccontextmanager async def build_runtime(settings) -> AsyncIterator[DiscordRuntime]:
      engine = build_engine(str(settings.database.url)); sessionmaker = build_session_factory(engine)
      async with AsyncAnthropic(api_key=settings.anthropic.api_key.get_secret_value(), base_url=str(settings.anthropic.base_url)) as anthropic:
          try: yield DiscordRuntime(settings, anthropic, sessionmaker)
          finally: await engine.dispose()
</interfaces>

<deploy_notes>
- fly.toml: app = "herald-scraper-bot-test", primary_region = "sea". `[deploy] release_command = "alembic upgrade head"`, strategy rolling. `[processes] discord = "python -m daimon.adapters.discord"`. `[checks] discord_liveness` type http port 8081 path "/" processes ["discord"]. `[env] DAIMON_LOG__LEVEL = "INFO"`. NO [http_service], NO mcp/scheduler/slack. `[[vm]]` shared-cpu-1x, 512mb, processes ["discord"]. Do NOT set app = "herald-scraper-bot" (that is PROD).
- entrypoint.sh drops `daimon defaults apply` entirely (Pitfall 1) — but keep it a valid entrypoint that `exec "$@"`s the fly.toml process command, and Alembic's release_command still works (it runs `alembic upgrade head` through the same entrypoint).
- Dockerfile: drop `COPY --chown=... defaults/ ./defaults/` (defaults dir excluded per D-04). Keep the uv multi-stage build, keep `COPY alembic.ini`, keep copying the entrypoint. CMD stays absent (fly.toml processes supply the command).
</deploy_notes>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the minimal mention→pong Discord adapter + trimmed runtime + entrypoint (with unit test)</name>
  <read_first>
    - /home/clsandoval/cs/daimon-cma-open-source/packages/adapters/discord/daimon/adapters/discord/__main__.py
    - /home/clsandoval/cs/daimon-cma-open-source/packages/adapters/discord/daimon/adapters/discord/runtime.py (first 60 lines)
    - /home/clsandoval/cs/daimon-cma-open-source/packages/adapters/discord/pyproject.toml
    - /home/clsandoval/cs/daimon-cma-open-source/docker/entrypoint.sh
    - packages/core/daimon/core/config.py (from plan 01 — Settings shape)
    - packages/core/daimon/core/health.py (from plan 01 — start_liveness_responder signature)
    - .planning/phases/01-clean-platform-skeleton/01-RESEARCH.md (Code Examples: "Minimal mention→pong Discord handler"; Pitfall 1)
  </read_first>
  <behavior>
    - Test 1: on_message with the bot in message.mentions → message.channel.send called once with "pong"
    - Test 2: on_message where bot NOT in message.mentions → send is never called (DISC-01)
    - Test 3: on_message where message.author == bot.user (self) → send is never called
  </behavior>
  <action>
    Create `packages/adapters/discord/pyproject.toml` (name `daimon-adapter-discord`, dep on
    daimon-core + discord.py>=2.7,<3 + anthropic + sqlalchemy[asyncio] + asyncpg + structlog;
    drop python-ulid/sentry unless imported). Write `bot.py` as the minimal `HeraldBot`
    (subclass `discord.Client`, on_ready logs, on_message applies the self-check + mention-check +
    `await message.channel.send("pong")` per the interfaces block). Write `runtime.py` as the
    trimmed `DiscordRuntime` dataclass + `build_runtime` asynccontextmanager (engine → sessionmaker
    + AsyncAnthropic only; no billing/notebook/resolver/defaults). Adapt `__main__.py` from
    daimon's: `load_settings()`, exit 0 if `settings.discord is None`, `configure_log_level`,
    `build_runtime`, set `intents = discord.Intents.default(); intents.message_content = True`,
    instantiate `HeraldBot(runtime=runtime, intents=intents)`, start the liveness responder on
    `settings.discord.health_port`, `await bot.start(settings.discord.bot_token.get_secret_value())`,
    close the health server in `finally`. DROP the Sentry/observability init block entirely.
    Add `__init__.py` files. Rewrite `docker/entrypoint.sh` to `#!/bin/sh` + `set -e` +
    `exec "$@"` (no `daimon defaults apply`). Write the unit test
    `packages/adapters/discord/tests/test_mention_pong.py` using unittest.mock: build a fake
    `discord.Message` (mock `.author`, `.mentions`, `.channel.send` AsyncMock), a HeraldBot with
    a mocked `.user`, and assert the three behaviors above. Register the discord adapter in the
    root `pyproject.toml` `[tool.uv.sources]` if not already, then `uv sync --no-dev`.
  </action>
  <verify>
    <automated>cd /home/clsandoval/cs/herald-scraper-bot && uv sync --no-dev && uv run python -c "import daimon.adapters.discord.bot, daimon.adapters.discord.runtime" && uv run pytest packages/adapters/discord/tests/test_mention_pong.py -q</automated>
  </verify>
  <done>HeraldBot replies pong only on mention (not self, not un-mentioned); runtime builds engine+sessionmaker+anthropic; __main__ starts liveness responder; entrypoint has no `defaults apply`; unit test green.</done>
</task>

<task type="auto">
  <name>Task 2: Copy inert scheduler package + create empty Alembic baseline migration</name>
  <read_first>
    - /home/clsandoval/cs/daimon-cma-open-source/packages/adapters/scheduler/pyproject.toml
    - /home/clsandoval/cs/daimon-cma-open-source/packages/adapters/scheduler/daimon/adapters/scheduler/main.py
    - /home/clsandoval/cs/daimon-cma-open-source/packages/adapters/scheduler/daimon/adapters/scheduler/settings.py
    - packages/core/alembic/env.py (from plan 01 — target_metadata = Base.metadata)
    - packages/core/daimon/core/_models.py (from plan 01 — empty Base)
    - .planning/phases/01-clean-platform-skeleton/01-RESEARCH.md (Open Question 2: scheduler copied inert, NOT in fly.toml processes)
  </read_first>
  <action>
    Copy the scheduler package tree into `packages/adapters/scheduler/` (pyproject.toml, main.py,
    settings.py, __init__.py) per D-04 — kept for Phase 3 reuse but NOT wired into fly.toml's
    processes and NOT run in Phase 1. If scheduler's `settings.py` references config sections that
    were trimmed out of core config.py in plan 01, gate/stub those references so the package
    imports cleanly (it does not run this phase). Register it in the root `pyproject.toml`
    `[tool.uv.sources]` as `{ workspace = true }`. Generate the baseline migration:
    with a scratch/local `DAIMON_DATABASE__URL` (postgresql+asyncpg) run
    `uv run alembic revision --autogenerate -m baseline` — since `_models.py` Base is empty, the
    resulting `packages/core/alembic/versions/0001_baseline.py` MUST have empty `upgrade()`/
    `downgrade()` bodies (`pass`), establishing the migration chain head without creating tables.
    Rename the file to a stable `0001_baseline.py` if autogenerate produced a hash-prefixed name.
    Confirm `uv run alembic upgrade head` applies cleanly against a local Postgres.
  </action>
  <verify>
    <automated>cd /home/clsandoval/cs/herald-scraper-bot && uv sync --no-dev && uv run python -c "import daimon.adapters.scheduler" && test -f packages/core/alembic/versions/0001_baseline.py && uv run python -c "import ast,glob; f=glob.glob('packages/core/alembic/versions/0001_baseline.py')[0]; src=open(f).read(); assert 'def upgrade' in src; print('OK')"</automated>
  </verify>
  <done>Scheduler package imports (inert, not in fly.toml); baseline migration file exists with an empty schema and applies via `alembic upgrade head`.</done>
</task>

<task type="auto">
  <name>Task 3: Adapt Dockerfile / fly.toml / docker-compose / .env.example and boot locally</name>
  <read_first>
    - /home/clsandoval/cs/daimon-cma-open-source/Dockerfile
    - /home/clsandoval/cs/daimon-cma-open-source/fly.toml
    - /home/clsandoval/cs/daimon-cma-open-source/docker-compose.yml
    - /home/clsandoval/cs/daimon-cma-open-source/.env.example
    - docker/entrypoint.sh (from task 1 — simplified)
    - .planning/phases/01-clean-platform-skeleton/01-RESEARCH.md (Pattern 3: release_command; deploy_notes above; Pitfall 1)
  </read_first>
  <action>
    Adapt the multi-stage `Dockerfile` from daimon's: keep the uv builder + slim runtime stages,
    keep `COPY alembic.ini`, keep copying `packages/` and the simplified `docker/entrypoint.sh`,
    but DROP `COPY --chown=... defaults/ ./defaults/` (defaults excluded per D-04). No CMD
    (fly.toml processes supply the command). Write `fly.toml` per the deploy_notes block:
    `app = "herald-scraper-bot-test"`, `primary_region = "sea"`,
    `[deploy] release_command = "alembic upgrade head"` strategy rolling, single
    `[processes] discord = "python -m daimon.adapters.discord"`, a `[checks] discord_liveness`
    http check on port 8081 path "/" for processes ["discord"], `[env] DAIMON_LOG__LEVEL = "INFO"`,
    and `[[vm]]` shared-cpu-1x/512mb/processes ["discord"]. Do NOT reference the prod app name
    `herald-scraper-bot`, and do NOT add mcp/scheduler/slack processes or [http_service].
    Adapt `docker-compose.yml` to just a `postgres` service + a `discord` service (build from the
    Dockerfile, `DAIMON_DATABASE__URL=postgresql+asyncpg://...@postgres:5432/...`,
    `DAIMON_DISCORD__BOT_TOKEN`/`DAIMON_ANTHROPIC__API_KEY` from env) with a
    depends-on/healthcheck so migrations can run before the bot; drop mcp/slack/init-provisioning.
    Write `.env.example` documenting DAIMON_DATABASE__URL, DAIMON_ANTHROPIC__API_KEY,
    DAIMON_DISCORD__BOT_TOKEN, DAIMON_LOG__LEVEL with placeholder (NON-secret) values only.
    Boot locally: `docker compose up` with a local Postgres, confirm the image builds, the
    entrypoint runs without a `daimon defaults apply` error, and Alembic `upgrade head` succeeds
    against the compose Postgres. (The Discord login will fail without a real token locally — that
    is expected; the boot verification target here is image build + migration success, NOT a live
    Discord connection. The live @bot→pong is plan 03's checkpoint.)
  </action>
  <verify>
    <automated>cd /home/clsandoval/cs/herald-scraper-bot && grep -q 'herald-scraper-bot-test' fly.toml && ! grep -q 'app *= *"herald-scraper-bot"' fly.toml && grep -q 'release_command' fly.toml && ! grep -q 'defaults apply' docker/entrypoint.sh && docker compose config >/dev/null && echo OK</automated>
  </verify>
  <done>Dockerfile builds without the defaults dir; fly.toml targets herald-scraper-bot-test (not prod) with a discord process + release migration; docker-compose brings up postgres+discord; local `alembic upgrade head` succeeds through the simplified entrypoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord user → bot | Untrusted message content crosses into the bot; only mention detection is trusted |
| container entrypoint → process | Entrypoint must not run privileged provisioning against external APIs |
| env/secrets → container | Tokens injected via env only, never baked into the image or fly.toml |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | on_message reacting to non-mentions | mitigate | Mention-gate (`self.user not in message.mentions` → return) enforces DISC-01; unit test asserts no send when un-mentioned or self-authored |
| T-02-02 | Denial of Service / Info Disclosure | `daimon defaults apply` calling live Anthropic API on every boot | mitigate | Rewrite entrypoint to `exec "$@"` (drop defaults apply); verify grep shows no `defaults apply` |
| T-02-03 | Information Disclosure | Secrets baked into image / fly.toml | mitigate | No secret literals in Dockerfile/fly.toml/.env.example; all via Fly secrets/env (verified by grep); .env.example carries placeholders only |
| T-02-04 | Tampering | Accidentally deploying over prod `herald-scraper-bot` | mitigate | fly.toml app name pinned to `herald-scraper-bot-test`; verify asserts prod name absent |
</threat_model>

<verification>
- `uv run pytest packages/adapters/discord/tests/test_mention_pong.py -q` passes (pong iff mentioned)
- `uv run python -c "import daimon.adapters.discord.bot, daimon.adapters.scheduler"` succeeds
- Baseline migration exists with empty upgrade/downgrade; `alembic upgrade head` clean
- Docker image builds; `docker compose up` runs migrations via the simplified entrypoint
- `fly.toml` names `herald-scraper-bot-test`, not `herald-scraper-bot`; has release_command + discord process
- No `daimon defaults apply` in entrypoint; no secret literals in Dockerfile/fly.toml/.env.example
</verification>

<success_criteria>
- Minimal Discord adapter replies pong only on mention (PLAT-01, DISC-01 at code level)
- Scheduler package present but inert (PLAT-01, kept for Phase 3)
- Empty baseline migration applies on `alembic upgrade head` (PLAT-04)
- Image builds + local compose boots + migrations run, no defaults-apply crash (PLAT-04)
- Deploy config isolated to the test app, not prod
</success_criteria>

<output>
Create `.planning/phases/01-clean-platform-skeleton/01-02-SUMMARY.md` when done
</output>
