---
phase: quick-260712-6p2
plan: 01
subsystem: database
tags: [sqlite, dota, scoring, pmi, discord, sql]

requires:
  - phase: spike-skill-weirdness
    provides: validated positional-surprisal scorer (pmi2.py + rules.py + tune.py) and the mode-conditioned/pool-filtered/distinct-top-3 design
provides:
  - Ranked-only (lobby_type = 7), >=50min (DUR_MIN=3000) discovery predicate replacing the old 30-60min band-skip
  - score_skill_weirdness(conn) ported into ingest.py, wired into --weirdness and the loop's 50-cycle rescore hook
  - skill_weirdness/skill_notes columns + per-player "dplus" (Dota Plus mastery level) in match_view/load_matches
  - Board sort (skillweird, mastery), filter (grandmaster Master+), and two new focus-view receipt sections
affects: [herald-board, ingest-loop]

tech-stack:
  added: []
  patterns:
    - "Sibling scorer pattern: score_skill_weirdness mirrors score_weirdness's streaming-pass + executemany shape (never holds the corpus in memory on the 1GB VM)"
    - "Corpus-relative p99 NOTE_BAR computed per-run (not hardcoded), consistent with the item scorer's aggregation but dynamic threshold"

key-files:
  created:
    - spikes/menu-v2/assets/ability_ids.json
  modified:
    - scripts/ingest.py
    - spikes/menu-v2/live_board.py
    - spikes/menu-v2/render.py

key-decisions:
  - "Ranked-only + >=50min discovery pivot (mid-flight locked decisions A+B): single DUR_MIN constant replaces DUR_SKIP_LO/HI band-skip"
  - "RETENTION_DAYS default bumped 10 -> 14 (locked addendum, arrived after plan was written)"
  - "Mode-keyed scorer stats + SINGLE_DRAFT/RANDOM_DRAFT exclusion kept as deliberate no-op robustness even though new discovery is single-mode (ALL_PICK_RANKED only)"

patterns-established:
  - "New corpus-relative scorer columns (skill_weirdness/skill_notes) follow the exact ALTER-TABLE no-op migration + streaming-pass + executemany pattern established by weirdness/weird_notes"

requirements-completed: [QUICK-260712-6p2]

duration: 25min
completed: 2026-07-12
---

# Quick Task 260712-6p2: Skill-Order Weirdness + Grandmaster Badge Summary

**Ported the validated positional-surprisal skill-order scorer into production ingest as a sibling of the item weirdness scorer, pivoted discovery to ranked-only >=50min matches, and surfaced both skill weirdness and Dota Plus grandmaster badges as new board sorts/filters/receipts.**

## Performance

- **Duration:** ~25 min
- **Completed:** 2026-07-12T05:14:09Z
- **Tasks:** 3 (2 code tasks + 1 verification-only task)
- **Files modified:** 3 (+1 vendored asset)

## Accomplishments
- Discovery SQL now queues only ranked (`lobby_type = 7`) matches lasting >= 50 minutes (`DUR_MIN=3000`), replacing the old "skip the 30-60 min band" predicate; `dur_boring()` enforces the same bar at enrich time.
- `score_skill_weirdness(conn)` ports the spike's validated scorer (pool-filtered positional surprisal, corpus-derived ults, distinct-ability top-3, 0.5x early-ult discount, p99 NOTE_BAR, human-readable receipts) as a streaming, memory-bounded sibling of `score_weirdness()`.
- `skill_weirdness`/`skill_notes` columns migrate cleanly on existing DBs; wired into both `--weirdness` and the loop's every-50-cycles rescore hook.
- Per-player Dota Plus mastery (`dplus`, `dotaPlus.level` only — never the `dotaPlusHeroXp` sentinel) now flows through `match_view()`/`load_matches()`.
- Board offers a `skillweird` sort, a `mastery` sort, a Master+ (`>=26`) grandmaster filter, and two new focus-view receipt sections (weird skill orders, per-player GM badges).
- All offline self-tests pass, including a new `--skillcheck` synthetic-corpus self-test proving SINGLE_DRAFT scores 0, weird > normal, receipt shape, and dplus survival — with no network/env dependency.

## Task Commits

Each task was committed atomically:

1. **Task 1: Ranked/>=50min discovery pivot + port skill-weirdness scorer into ingest.py** - `8cd58f9` (feat)
2. **Task 2: Surface skill-weirdness + Dota Plus mastery on the board** - `4eb8614` (feat)
3. **Task 3: Local end-to-end verification (no deploy)** - no code changes; all verify steps passed on first run, nothing to commit

**Plan metadata:** (this SUMMARY + STATE/ROADMAP update — handled by orchestrator's docs commit)

## Files Created/Modified
- `spikes/menu-v2/assets/ability_ids.json` (new) - vendored ability id -> name map (3150 entries, copied from `spikes/skill-weirdness/ability_ids.json`)
- `scripts/ingest.py` - discovery pivot (DUR_MIN, lobby_type=7), skill_weirdness/skill_notes columns, `match_view()` dplus key, `score_skill_weirdness()`, `--skillcheck`, RETENTION_DAYS 10->14
- `spikes/menu-v2/live_board.py` - `skillweird`/`mastery` SORTS, `mastery` FILTER, two new `_focus()` receipt sections
- `spikes/menu-v2/render.py` - `load_matches()` dplus key (kept in sync with `ingest.match_view()`)

## Decisions Made
- Kept the mode-keyed `(hero, mode, idx)` stats and the `SINGLE_DRAFT`/`RANDOM_DRAFT` exclusion list even though the new ranked-only discovery makes them effectively no-ops on fresh data — cheap robustness for pre-purge rows and any future mode drift (per locked decision A).
- Folded the mid-flight `RETENTION_DAYS` default change (10 -> 14) into Task 1's discovery-pivot commit per the plan addendum, and left the nearby ponytail comment (`"not 2 days"`) unchanged since it doesn't reference the old numeric value.
- Left `live_board.py`'s header comment ("10-day retention") untouched — it's outside this plan's `files_modified` list and out of scope for this task.

## Deviations from Plan

None - plan executed exactly as written, including the mid-flight RETENTION_DAYS addendum folded into Task 1.

## Issues Encountered
- No local `.venv` existed inside the git worktree (worktrees don't get their own venv). Used the main repo's `.venv` (`/home/clsandoval/cs/herald-scraper-bot/.venv`) for all verification commands — same filesystem, no isolation concern since only read/execute was needed, no packages were modified.
- The real local `herald.db` also lives in the main repo directory, not the worktree; `HERALD_DB=/home/clsandoval/cs/herald-scraper-bot/herald.db` was used for the Task 3 board-import smoke test and the real-DB `--weirdness` run. No other process was touching the DB at the time (verified via `ps aux`), so this was safe.

## Verification Evidence

```
$ python3 scripts/ingest.py --selfcheck
selfcheck OK: 38 matches / 380 players

$ python3 scripts/ingest.py --skillcheck
skillcheck OK

$ grep -q "lobby_type = 7" scripts/ingest.py && grep -q "DUR_MIN" scripts/ingest.py && ! grep -q "DUR_SKIP" scripts/ingest.py && echo "discovery pivot OK"
discovery pivot OK

$ HERALD_DB=/home/clsandoval/cs/herald-scraper-bot/herald.db python3 -c "... assert 'skillweird' in SORTS and 'mastery' in SORTS and 'mastery' in FILTERS ..."
board wiring OK

$ HERALD_DB=/home/clsandoval/cs/herald-scraper-bot/herald.db python3 scripts/ingest.py --weirdness
weirdness: 18666 matches scored
skill weirdness: 18666 matches scored
```

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Ready for the separate, manual deploy step documented in the plan's `<output>` section:
1. `fly deploy -c fly.board.toml`
2. One-off purge of now-nonconforming corpus rows on prod (non-ranked / <50min matches)
3. Mandatory rescore: `python3 scripts/ingest.py --weirdness` on prod (scores both item and skill weirdness — required because the purge changes the corpus the PMI stats are drawn from)

No blockers. All offline checks (`--selfcheck`, `--skillcheck`) and the real-DB smoke test pass locally.

---
*Phase: quick-260712-6p2*
*Completed: 2026-07-12*

## Self-Check: PASSED

All created/modified files confirmed present; both task commits (`8cd58f9`, `4eb8614`) confirmed in git log.
