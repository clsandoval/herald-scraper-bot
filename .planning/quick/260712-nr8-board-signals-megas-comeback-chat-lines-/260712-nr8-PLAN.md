---
phase: quick-260712-nr8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ingest.py
  - spikes/menu-v2/render.py
  - spikes/menu-v2/live_board.py
  - spikes/menu-v2/check_receipts.py
autonomous: true
requirements: [SIGNAL-MEGAS, SIGNAL-CHAT, SIGNAL-RECEIPTS, SIGNAL-SPOILER]

must_haves:
  truths:
    - "matches table has megas_comeback + chat_lines columns, backfillable via --recompute (no Stratz cost)"
    - "megas_comeback=1 exactly when the winner's own side had all barracks razed"
    - "chat_lines = total all-chat lines across all players in the match"
    - "Board offers a 'Comeback from mega creeps' filter and a 'Most talkative' sort"
    - "Focus view shows per-player feeding + shame receipts when thresholds are met"
    - "Under spoiler mode the megas match tag is hidden; per-player receipts stay visible"
  artifacts:
    - path: "scripts/ingest.py"
      provides: "megas_comeback + chat_lines derived columns, schema + migration + upsert wiring, signalcheck() offline test"
      contains: "megas_comeback"
    - path: "spikes/menu-v2/render.py"
      provides: "player_receipts() + megas_tag() pure helpers (offline-importable)"
      contains: "def player_receipts"
    - path: "spikes/menu-v2/live_board.py"
      provides: "'talkative' SORT, 'megas' FILTER, focus-view receipt blocks gated by spoiler"
      contains: "megas_comeback"
    - path: "spikes/menu-v2/check_receipts.py"
      provides: "offline assertion of receipt thresholds + spoiler neutrality"
  key_links:
    - from: "scripts/ingest.py derived_cols()"
      to: "matches.megas_comeback / matches.chat_lines"
      via: "recompute() UPDATE (auto-includes new derived_cols keys)"
      pattern: "megas_comeback"
    - from: "spikes/menu-v2/live_board.py _focus()"
      to: "render.player_receipts / render.megas_tag"
      via: "raw JSON query at render, spoiler-gated"
      pattern: "player_receipts"
---

<objective>
Surface the VALIDATED signal-mining spike findings on the live board: two new
recompute-able derived columns (`megas_comeback`, `chat_lines`), one filter
("Comeback from mega creeps"), one sort ("Most talkative"), and spoiler-aware
per-player focus-view receipts (feeding + item/random shame + megas match tag).

Purpose: give Jenkins new ways to find review-worthy Herald disasters (won
while megged; chat-heavy lobbies; feeders/BKB-never-pressed/randomers) without
extra Stratz cost — all fields already live in stored raw JSON.

Output: derived columns + backfill path, board sort/filter, focus receipts,
plus two offline assertion harnesses. Deploy is manual (see <output>).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
Ponytail mode (CLAUDE.md): minimal diffs, stdlib only, no new deps, dicts
throughout, follow surrounding untyped style. Match existing patterns exactly.

@.planning/spikes/signal-mining/README.md   # VALIDATED spike — thresholds locked
@scripts/ingest.py                            # derived_cols/upsert/init_db/recompute/skillcheck patterns
@spikes/menu-v2/live_board.py                 # SORTS/FILTERS dicts, _focus receipt blocks (lines ~414-447), spoiler handling (sp = self.st.get("spoiler"))
@spikes/menu-v2/render.py                     # pure render helpers (hero_emoji, dur); importable offline, no discord/env

<interfaces>
LOCKED constants: BKB item id = 116, Midas item id = 65.

STRATZ_FIELDS (already ingested — all receipt inputs are in stored raw):
  match: barracksStatusRadiant, barracksStatusDire, didRadiantWin
  player: isRandom
  player.stats: allTalks [{time,message}], itemPurchases [{time,itemId}],
                itemUsed [{itemId,count}],
                deathEvents [{time,timeDead,goldFed,goldLost,isDieBack,isAttemptTpOut,hasHealAvailable}]

scripts/ingest.py derived_cols(raw) returns a dict; recompute() UPDATEs every
key it returns, so NEW keys are backfilled automatically — no recompute change.
upsert_match() has a HARDCODED column list + VALUES + params tuple — new columns
MUST be added in all three spots there AND in CREATE TABLE + the init_db ALTER
migration list.

spikes/menu-v2/live_board.py:
  SORTS = { key: (label, direction-neutral ORDER BY expr) }  # ~line 135
  FILTERS = { key: (label, WHERE expr, matches.-qualified) }  # ~line 160
  FILT_COUNTS computes sum(expr) per filter at startup — expr must be sum-able.
  _focus(): sp = self.st.get("spoiler"); existing receipt blocks read precomputed
    columns and append discord.ui.TextDisplay to container c. Match that shape.

render.py loads only json/os + assets at import (no discord, no env) → safe to
import from an offline assertion script. hero_emoji(hid)/hero_name(hid)/dur(s)
already exist.
</interfaces>

LOCKED thresholds (from spike, do NOT retune):
- feeding: per player when summed timeDead >= 1200s OR >= 33% of match duration.
  Line: "X min (Y%) dead, fed Zk gold" (timeDead/goldFed summed from deathEvents).
- BKB bought never used: itemPurchases has 116 with time>0 AND itemUsed count for 116 == 0.
- Midas: bought (65, time>0) AND itemUsed count for 65 < 10 → "Midas, <N> uses".
- randomed: player isRandom truthy.
- died mid-TP xN: count of deathEvents with isAttemptTpOut, show when N >= 2.
- buyback then died xN: count of deathEvents with isDieBack, show when N >= 2.
- megas_comeback: winner's own side barracksStatus == 0 (won while megged) ~6.7% of corpus.
- SORT "Most talkative" = coalesce(chat_lines, 0). NO chat excerpts, NO translation (locked).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add megas_comeback + chat_lines derived columns to ingest</name>
  <files>scripts/ingest.py</files>
  <action>
Add two recompute-able derived columns per LOCKED decision 1.

1. In `derived_cols(raw)`: compute
   - `megas_comeback` = 1 if the winner's own-side barracks bitmask == 0 else 0.
     Winner side chosen by `raw["didRadiantWin"]`: use `raw.get("barracksStatusRadiant")`
     when radiant won, else `raw.get("barracksStatusDire")`. Compare `== 0` (None/missing
     → not megged → 0; the old fixture lacks these fields, which is fine).
   - `chat_lines` = sum of `len((p.get("stats") or {}).get("allTalks") or [])` over `raw["players"]`.
   Add both keys to the dict `derived_cols` returns.
2. In `init_db` CREATE TABLE matches: append `megas_comeback INTEGER, chat_lines INTEGER`.
3. In the `init_db` ALTER migration list: add `("matches", "megas_comeback INTEGER")`
   and `("matches", "chat_lines INTEGER")` so pre-existing prod DBs gain the columns.
4. In `upsert_match`: add `megas_comeback, chat_lines` to the INSERT column list, add two
   `?` placeholders to VALUES, and add `c["megas_comeback"], c["chat_lines"]` to the params
   tuple (order-matched).
5. Do NOT touch `recompute()` — it UPDATEs every key `derived_cols` returns, so the two
   new columns backfill automatically. Do NOT touch `match_view`/`load_matches` or the
   `selfcheck()` EXPECTED_KEYS set (match_view is unchanged; the board reads these two
   values as columns, not via match_view).
6. Add a `signalcheck()` function + `--signalcheck` CLI flag, mirroring the existing
   `skillcheck()` structure (offline, no env, `get_conn(":memory:")`, exit code). Build 3
   synthetic matches via `upsert_match`:
   - radiant win, barracksStatusRadiant=0 → assert stored megas_comeback == 1
   - radiant win, barracksStatusRadiant=63 → assert megas_comeback == 0
   - dire win, barracksStatusDire=0 → assert megas_comeback == 1
   Give players `stats.allTalks` lists summing to a known total; assert stored chat_lines
   equals that total. Then call `recompute(conn)` and re-assert both columns are unchanged
   (proves the backfill path). Print "signalcheck OK" / "signalcheck FAILED: {e}".
  </action>
  <verify>
    <automated>cd /home/clsandoval/cs/herald-scraper-bot && python3 -m py_compile scripts/ingest.py && python3 scripts/ingest.py --signalcheck && python3 scripts/ingest.py --selfcheck && python3 scripts/ingest.py --skillcheck</automated>
  </verify>
  <done>signalcheck, selfcheck, and skillcheck all print OK and exit 0; py_compile clean. megas_comeback/chat_lines present in CREATE TABLE, migration list, upsert INSERT+VALUES+params, and derived_cols return.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Board sort, filter, and spoiler-aware focus receipts</name>
  <files>spikes/menu-v2/render.py, spikes/menu-v2/live_board.py, spikes/menu-v2/check_receipts.py</files>
  <behavior>
    render.player_receipts(rawp, duration_s) → list[str], winner-neutral:
    - timeDead sum 1300s (>=1200) → includes "21 min (…%) dead, fed …k gold"
    - timeDead 300s, frac<0.33 → no feeding line
    - itemPurchases has {itemId:116,time:600}, itemUsed lacks 116 → "BKB bought, never used"
    - bought 65 (time>0), itemUsed count 5 for 65 → "Midas, 5 uses"
    - isRandom truthy → "randomed"
    - 2 deathEvents with isAttemptTpOut → "died mid-TP x2"; 1 → absent
    - 2 deathEvents with isDieBack → "buyback then died x2"; 1 → absent
    render.megas_tag(raw) → str|None (OUTCOME-REVEALING):
    - didRadiantWin=True + barracksStatusRadiant=0 → non-None string
    - barracksStatusRadiant=63 → None
  </behavior>
  <action>
Surface the signals on the board per LOCKED decisions 2–6.

render.py (pure helpers, offline-importable — put near the other rendering helpers):
- Add module constants `BKB_ID = 116`, `MIDAS_ID = 65`.
- `player_receipts(rawp, duration_s)`: return a list of winner-NEUTRAL receipt strings
  for one raw Stratz player dict, applying the LOCKED thresholds in <context>. Sum
  timeDead/goldFed from `stats.deathEvents`; feeding line "`{timeDead//60} min ({round(frac*100)}%) dead, fed {goldFed//1000}k gold`"
  when timeDead>=1200 or frac>=0.33 (frac = timeDead/duration_s). Build a
  {itemId:count} map from `stats.itemUsed`; "bought" = any itemPurchases entry with that
  itemId and time>0. Append BKB / Midas / randomed / died-mid-TP / buyback-then-died tags
  per thresholds. Return [] when nothing qualifies. These strings NEVER reveal the winner.
- `megas_tag(raw)`: return an outcome-revealing string (e.g. "🔥 Won from mega creeps —
  the winning team's barracks were all razed") when the winner's own barracks bitmask
  (barracksStatusRadiant if didRadiantWin else barracksStatusDire) == 0, else None.
  Document in the docstring that callers MUST hide it under spoiler mode.

live_board.py:
- SORTS: add `"talkative": ("Most talkative (all-chat lines)", "coalesce(chat_lines, 0)")`.
  (Direction-neutral; default DESC via existing PREF_DIR fallback — no PREF_DIR entry needed.)
- FILTERS: add `"megas": ("Comeback from mega creeps", "matches.megas_comeback = 1")`.
  Confirm FILT_COUNTS' `sum(matches.megas_comeback = 1)` works (boolean→0/1 in SQLite).
- _focus(): after the existing mastery block and before the MediaGallery, query the raw
  JSON once (`q("SELECT raw FROM matches WHERE match_id=?", (m["id"],))`, `json.loads`).
  For each player call `render.player_receipts(rawp, m["duration"])`; collect
  `{hero_emoji or hero_name} {"; ".join(receipts)}` lines for players with any receipts and
  add one TextDisplay block (e.g. "💀 **Feeding & shame**") when non-empty. Then, ONLY when
  `not sp` (sp = self.st.get("spoiler")), compute `render.megas_tag(raw)` and, if non-None,
  add it as a TextDisplay. Per-player receipts render regardless of `sp` (they are
  winner-neutral); the megas tag is suppressed under spoiler (LOCKED decision 5).

check_receipts.py (NEW, offline, no discord/env — import ONLY `render`, not live_board;
live_board opens the DB + builds TEMP tables at import and is not offline-importable):
- Build synthetic raw player dicts and a synthetic raw match covering every <behavior>
  case; assert player_receipts contents (membership + absence) and megas_tag non-None/None.
- Assert the spoiler contract structurally: no string returned by player_receipts contains
  "win"/"Won"/mega wording (they are winner-neutral), while megas_tag DOES — i.e. the two
  helpers are cleanly separated so _focus can gate only megas_tag on `not sp`.
- Print "check_receipts OK" / "check_receipts FAILED: {e}" with a nonzero exit on failure,
  runnable as `python3 spikes/menu-v2/check_receipts.py`.
  </action>
  <verify>
    <automated>cd /home/clsandoval/cs/herald-scraper-bot && python3 -m py_compile spikes/menu-v2/render.py spikes/menu-v2/live_board.py spikes/menu-v2/check_receipts.py && python3 spikes/menu-v2/check_receipts.py && grep -q 'coalesce(chat_lines' spikes/menu-v2/live_board.py && grep -q 'megas_comeback = 1' spikes/menu-v2/live_board.py</automated>
  </verify>
  <done>check_receipts prints OK exit 0; py_compile clean for all three files; SORTS has "talkative", FILTERS has "megas"; _focus renders per-player receipts always and the megas tag only when not spoiler.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| board → viewer (public #replays) | rendered focus view; spoiler mode is the only confidentiality control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-signals-01 | Information Disclosure | _focus() megas match tag | mitigate | render megas_tag ONLY when `not sp`; per-player receipts are winner-neutral by construction; check_receipts.py asserts the two helpers are cleanly separated |
| T-signals-02 | Tampering | new SORT/FILTER SQL | accept | direction-neutral expr + `= 1` literal, no user input interpolated; consistent with existing SORTS/FILTERS |

No new packages, no network, no new external boundary — read-only board + stored raw JSON only.
</threat_model>

<verification>
- `python3 scripts/ingest.py --signalcheck` → OK (columns computed + backfilled via recompute)
- `python3 scripts/ingest.py --selfcheck` and `--skillcheck` → still OK (no regression)
- `python3 spikes/menu-v2/check_receipts.py` → OK (thresholds + spoiler separation)
- `py_compile` clean for ingest.py, render.py, live_board.py, check_receipts.py
</verification>

<success_criteria>
- Two recompute-able columns added end-to-end (derived_cols → CREATE TABLE → migration → upsert), backfillable with zero Stratz cost
- "Comeback from mega creeps" filter and "Most talkative" sort live on the board
- Focus view shows spoiler-aware receipts: feeding + BKB/Midas/randomed/mid-TP/buyback tags per player (always), megas match tag (hidden under spoiler)
- All four LOCKED thresholds honored exactly; no chat excerpts/translation
- All offline checks green
</success_criteria>

<output>
Create `.planning/quick/260712-nr8-board-signals-megas-comeback-chat-lines-/260712-nr8-SUMMARY.md` when done.

MANUAL DEPLOY STEP (note for the human, LOCKED decision 7 — do NOT run here):
After deploying, run `python scripts/ingest.py --recompute` on the Fly `herald-board`
prod machine. This calls get_conn() (which runs init_db → ALTERs in the two new columns)
and backfills megas_comeback + chat_lines across the whole corpus from stored raw JSON —
NO Stratz cost. The board's startup FILT_COUNTS/SORTS queries reference these columns, so
recompute must complete before restarting the board process.
</output>
